name: Validate Blog Index

on:
  push:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate blog templates
        run: bash containers/blog/scripts/validate_blog_templates.sh
